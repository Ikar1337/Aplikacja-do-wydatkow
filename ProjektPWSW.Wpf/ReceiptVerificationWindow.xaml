<Window x:Class="ProjektPWSW.Wpf.ReceiptVerificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        Title="Weryfikacja Paragonu" Height="450" Width="700"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sprawdź i przypisz kategorie odczytanym pozycjom:" FontSize="16" FontWeight="Medium" Margin="0,0,0,15"/>

        <DataGrid x:Name="ItemsGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,15">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Nazwa na paragonie" Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Width="*"/>

                <DataGridTextColumn Header="Kwota" Binding="{Binding Amount, UpdateSourceTrigger=PropertyChanged}" Width="100"/>

                <DataGridTemplateColumn Header="Wybierz kategorię" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <ComboBox materialDesign:HintAssist.Hint="Kategoria..."
                                      ItemsSource="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=Categories}"
                                      DisplayMemberPath="Name"
                                      SelectedValuePath="Id"
                                      SelectedValue="{Binding CategoryId, UpdateSourceTrigger=PropertyChanged}"
                                      Margin="5,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Anuluj" Style="{StaticResource MaterialDesignOutlinedButton}" Click="Cancel_Click" Margin="0,0,10,0" Width="100"/>
            <Button Content="Zapisz wydatki" Click="Save_Click" Width="140"/>
        </StackPanel>
    </Grid>
</Window>
